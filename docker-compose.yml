version: '3.8'

services:
  claude-agent:
    image: xorio42/claude-agent:latest
    container_name: claude-agent
    network_mode: host
    stdin_open: true
    tty: true
    restart: unless-stopped
